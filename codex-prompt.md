# Codex 实现指令 — 比特海盗的码头

> 船长和 Trae 已经对架构、字段、流程、视觉方向做了一轮完整的探讨，形成了这份纪要。现在交给你（Codex），目的是：**进一步探讨未决的技术问题，然后落地实现。**
>
> **以下内容是探讨的产物，不是不可违逆的需求文档。** 你有自己的判断——如果觉得哪里不合理、有更好的方案、或者需要补充什么，直接说。你的专业意见是这一轮的核心价值。

---

## 项目背景（读一下，理解语境）

**我是谁：**
- 小红书账号"比特海盗"，大一学生，暑期创业实验记录者
- 正在做 100 天创业实验的连续剧式内容
- 今天是 2026-07-12，我受邀参加 Trae 线下 Workshop

**为什么做这个站：**
- 我在小红书积累了一批对 AI 动手感兴趣的读者
- 正在筹备建一个 AI 社群
- 这个站是社群的基础设施——**作品展示集合站**，名为"比特海盗的码头"
- 社群成员（海盗们）提交自己用 AI 做的作品，我审批后展示在码头上
- 今晚我会在小红书发帖，分享 Workshop 见闻，并透露"正在为社群搭一个码头"

**当前状态：**
- 社群还没正式开放，MVP 先跑通流程
- 前期核心目标：有作品上架，能展示，能分享

---

## 技术栈

- **前端：** 纯静态 HTML/CSS/JS（单页应用，多视图切换）
- **后端：** Supabase（数据库 + 文件存储 + API）
- **部署：** Cloudflare Pages
- **域名：** 船长自有，托管在 Cloudflare

---

## 视觉风格

前端设计由你结合前端 skill 来发挥，符合"比特海盗"的氛围感，精美有亮点，但不是一味元素的堆叠。**核心原则：** 航海元素为基底，海盗元素做点缀——有特点但不廉价。

结构要求见下方「页面结构」，视觉实现完全交给你判断。

---

## 数据库表结构（Supabase）

### 表名：`works`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键，默认 gen_random_uuid() |
| created_at | timestamptz | 提交时间，默认 now() |
| title | text | 作品名称 |
| description | text | 一句话简介 |
| author_name | text | 作者昵称 |
| author_role | text | 角色标签 |
| cover_url | text | 封面图 Storage URL |
| link | text | 作品链接，可空 |
| journey_log | text | 航程录（详细介绍） |
| tags | text[] | 标签数组，初始为空 |
| status | text | 'pending' / 'approved' / 'rejected' |
| review_note | text | 审批备注，可空 |

### 存储桶：`work-covers`
- 公开读取，限图片格式（jpg/png/webp）

### 行级安全（RLS）
- 公开可读：`status = 'approved'`
- 公开可插入：写入时 status 强制为 'pending'
- 更新/删除：仅审批后台可操作，具体实现策略见下方「待探讨问题 1」

---

## 页面结构（方向，你来细化）

### 1. 首页 — 作品列表
- 顶部：站点名 + 导航（全部作品 / 提交作品）
- 展示审核通过的作品，按时间倒序
- 网格布局，适配 PC/平板/手机
- 卡片内容：封面图、作品名称、一句话简介、作者昵称+角色 badge、标签
- 卡片点击 → 进入详情页
- 网格列数、卡片密度你来判断，以"浏览舒适"为准

### 2. 详情页
- 展示单个作品的完整信息
- 封面图、作品名称、简介、作者、角色 badge（不同角色不同颜色）
- 航程录全文
- 如果有外部链接，显示"去看看"按钮
- 底部：标签、提交时间
- 返回首页的链接

### 3. 提交页
- 表单字段：
  - 作品名称（input）
  - 一句话简介（input）
  - 作者昵称（input，首次填写后存 localStorage，下次自动填充，可手动修改）
  - 作者角色（下拉选择，固定列表，与昵称一并缓存）
  - 封面图（文件上传，仅一张）
  - 作品链接（input，选填）
  - 航程录（textarea，详细介绍）
- 提交后写入 Supabase，status = 'pending'
- 成功后显示提示信息

### 4. 审批后台
- 路径 `/admin`，进入需输入密码
- 列表展示 pending 作品，显示：作品名、作者、提交时间、封面缩略图、航程录预览
- 操作：**通过** → status 改为 approved / **驳回** → 弹窗输入理由后 status 改为 rejected
- 可切换查看"已通过" / "已驳回"列表

---

## 角色颜色（供参考，你可以调）

| 角色 | 参考色值 |
|------|----------|
| 船长 | 琥珀金系 |
| 大副 | 深绿系 |
| 舵手 | 海蓝系 |
| 领航员 | 青蓝系 |
| 炮手 | 砖红系 |
| 瞭望员 | 沙黄系 |
| 船医 | 暖红系 |
| 水手 | 灰蓝系 |

角色 badge 的颜色和风格你来判断，与整体视觉统一即可。

---

## 标签分类（审批时船长打标签）

预定义标签池（可扩展，你建议更合适的分类）：
- 网页 / 工具 / AI 应用 / 小程序 / 脚本 / 数据可视化 / 游戏 / 其他

---

## ⚠️ 待探讨的问题（以下需要你给出建议方案）

### 1. 审批后台的密码验证怎么落地？
这是纯静态页面，没有后端服务器。密码不能写死在 JS 里（不安全）。可能的方向：
- 或者你推荐其他方案？
- 安全要求不高，MVP 阶段防君子不防小人即可

### 2. 文件上传的前端实现
封面图从浏览器直传到 Supabase Storage，anon key 会暴露在前端。这个安全边界是否可接受？或者你有更好的做法？

### 3. AI 打标签的接入方式
审批通过时，希望 AI 自动建议标签。可能的方向：
- 前端直接调 DeepSeek / 硅基流动 API（API key 暴露问题）
- Supabase Edge Function 作为代理
- 还是 MVP 阶段先纯手动打标签，以后再上 AI？
- 请给出你的建议

### 4. 单页 vs 多页
4 个视图（首页/详情页/提交页/后台）做成一个 HTML 的 JS 切换视图，还是拆成多个页面？你的建议是什么，基于部署在 CF Pages 的考量。

---

## 实现顺序

1. 船长手动在 Supabase 后台创建项目，建表 `works` + 存储桶 `work-covers` + 配置 RLS（这一步需要你给出建表 SQL 和配置指引）
2. 首页 + 详情页
3. 提交页（含文件上传）
4. 审批后台
5. 联调完整流程

---

**最后：** 在动手之前，先看一下我给你的这些待探讨问题，给出你的方案和建议。如果觉得有需要补充或调整的地方，直接提。